/**
 * @content-forge/web
 * Scene-based multimodal video editor frontend
 *
 * Design System: Style L (Production Ready) — dark-first theme
 * Framework: Vue 3 (Composition API) + Tailwind CSS + Pinia
 *
 * Pages:
 * - Dashboard        — 프로젝트 목록 + 통계
 * - ProjectEditor    — 씬 타임라인 + 인스펙터 + 미리보기
 * - AvatarSettings   — AI 아바타 프로필 관리
 * - Settings         — 전역 설정 (API 키, 보이스, 스타일)
 *
 * Stores:
 * - projectStore     — VideoProject 상태 관리
 * - sceneStore       — EditableScene CRUD + 버전 히스토리
 * - avatarStore      — AvatarProfile 관리
 * - renderStore      — 렌더링 진행률 + 상태
 */

export {};
